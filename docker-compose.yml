version: '3.8'

services:
  db:
    image: mysql:latest
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: admin 
      MYSQL_DATABASE: parking
      MYSQL_USER: jazz
      MYSQL_PASSWORD: jazz
    ports:
      - "3306:3306"
    volumes:
      - ./data:/var/lib/mysql
